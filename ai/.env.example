# ─── LiteLLM ─────────────────────────────────────────────────────────────────
LITELLM_API_KEY=sk-...
LITELLM_BASE_URL=https://your-litellm-proxy.com/v1
LITELLM_MODEL=gpt-4o-mini

# ─── Graphiti knowledge graph ────────────────────────────────────────────────
GRAPHITI_URL=http://localhost:8000
GRAPHITI_GROUP_ID=predict

# ─── A2A server ───────────────────────────────────────────────────────────────
PORT=4337
BASE_URL=http://localhost:4337

# ─── User profiles (written by listener, read by ai) ─────────────────────────
# Must match listener PROFILES_DIR — both modules share the same data/ directory
PROFILES_DIR=../data/profiles
POSITIONS_DIR=../data/positions
AUDIT_DIR=../data/audit
MARKET_REGISTRY_FILE=../data/market_registry.json

# ─── Fallback wallet (used when no ENS profile is loaded) ────────────────────
AGENT_WALLET_ADDRESS=0x...

# ─── Daily cycle ──────────────────────────────────────────────────────────────
# How often to run the analysis-investment cycle (ms, default 24h)
CYCLE_INTERVAL_MS=86400000
# Set to "false" to enable live trading
DRY_RUN=true
